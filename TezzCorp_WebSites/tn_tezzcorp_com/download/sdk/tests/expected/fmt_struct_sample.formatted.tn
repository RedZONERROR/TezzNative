import "io"

struct Pair:
  left:i64
  right:i64

fn pair_sum(p:*Pair):
  unsafe:
    ret p[0].left + p[0].right

fn main():
  let pair:Pair
  pair.left = 7
  pair.right = 5
  unsafe:
    say(pair_sum(&pair))
  if io.write_all("build/fmt_pair.txt", "ok") < 0:
    ret 1
  ret 0

