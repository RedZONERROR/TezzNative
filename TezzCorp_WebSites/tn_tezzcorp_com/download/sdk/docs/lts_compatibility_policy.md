# LTS Compatibility Policy

## LTS Policy
- LTS branches receive stability and security fixes with no surprise syntax breaks.

## Compatibility Contract
- `systems` mode remains backward-compatible by default.
- New language ergonomics ship behind explicit opt-in (`--mode simple`).
